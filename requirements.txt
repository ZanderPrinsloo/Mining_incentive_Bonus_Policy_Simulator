Flask==3.1.0
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.1
blinker==1.9.0

pytest==8.2.0

# Optional: only used by storage/stptm_import.py (Doornkop "Import from
# STPTM9000" button). The app runs fine without either — that feature just
# hides itself when pyodbc, python-dotenv, or the SQL Server instance
# isn't available/configured. See .env.example for the connection settings.
pyodbc==5.3.0
python-dotenv==1.2.3

# Production WSGI server (run_web.py uses this when APP_ENV=production) — pure
# Python, no compiler needed, runs natively on Windows unlike gunicorn. Only
# imported in that mode; local dev (the default) doesn't need it installed.
waitress==3.0.2
